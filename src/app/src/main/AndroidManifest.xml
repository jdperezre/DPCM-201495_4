<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.culturapp.culturapp">

    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Culturappstyle">
        <activity android:name=".ui.login.ForgetPasswordActivity"></activity>
        <activity android:name=".ui.login.ForgetTokenActivity" />
        <activity android:name=".ui.login.RegisterUserInfoActivity" />
        <activity android:name=".ui.login.RegisterLoginInfoActivity" />
        <activity android:name=".ui.login.ForgetEmailActivity" />
        <activity
            android:name=".ui.login.LoginActivity"
            android:label="@string/app_name"
            android:screenOrientation="portrait"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ui.events.NewEventActivity" />
        <activity
            android:name=".ui.events.EventsActivity"
            android:screenOrientation="portrait" />
        <activity
            android:name=".LateralMenuActivity"
            android:screenOrientation="portrait" />
        <activity
            android:name=".SplashActivity"
            android:label="@string/app_name"
            android:screenOrientation="portrait"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".MainActivity"
            android:screenOrientation="portrait" />
    </application>

</manifest>